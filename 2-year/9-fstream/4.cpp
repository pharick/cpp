#include <fstream>
#include <cstring>
using namespace std;

int main() {
    ifstream in("in.txt");
    ofstream out("out.txt");

    char line[101] = {};

    short count = 0;
    while (!in.eof()) {
        in.get(symb);
        std::cout << smb;
    }

    in.close();
    out.close();
    return 0;
}